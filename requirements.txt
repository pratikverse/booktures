# Full local development install (API + local GPU/CPU inference stack).
# For a lean cloud/free-tier deployment, install only backend/requirements-api.txt
# and point the app at cloud LLM/image providers instead.
-r backend/requirements-api.txt
-r backend/requirements-local-gpu.txt
